// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // TODO: poner el dominio definitivo (comprado en Cloudflare) cuando esté decidido
  // site: "https://virginiamarin.com",
});
