<section id="inicio" class="hero ph" aria-label="Inicio">
  <h1>Virginia&nbsp;Marin</h1>
  <span class="hero-note">Imagen provisional — sustituir por la foto de portada de Canva</span>
</section>

<style>
  .hero {
    min-height: 72vh;
    border-radius: 0;
    background:
      radial-gradient(ellipse at 72% 85%, rgb(52 66 78 / 0.4), transparent 55%),
      linear-gradient(165deg, #aebfca 0%, #cfd9df 45%, #97aab8 100%);
  }

  h1 {
    font-family: var(--font-display);
    font-weight: 400;
    color: #fff;
    font-size: clamp(3.5rem, 10vw, 7rem);
    margin: 0;
    text-shadow: 0 2px 18px rgb(40 55 70 / 0.35);
  }

  .hero-note {
    position: absolute;
    right: 1rem;
    bottom: 0.8rem;
    font-size: 0.7rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgb(255 255 255 / 0.65);
    padding: 0;
    text-shadow: none;
  }
</style>
